flyer-tool split template

Run with a local static server (recommended):
  python -m http.server 8000

Then open:
  http://localhost:8000/flyer_tool_html/index.html

Note: Opening file:// directly may block ES modules & fetch().
　　
